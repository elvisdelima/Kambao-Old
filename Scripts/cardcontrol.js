$(document).ready(function () {
    $('.cartao').draggable({
        cursor: 'move',          // sets the cursor apperance
        opacity: 0.70            // opacity while the element is dragged
    });
});